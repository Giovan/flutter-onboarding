/* import 'package:flutter_onboarding/pages/home/home_page.dart';
import 'package:flutter_onboarding/pages/login/login_page.dart';
import 'package:flutter_onboarding/pages/signup/signup_page.dart';
import 'package:flutter_onboarding/pages/splash/splash.dart';

import 'package:flutter_onboarding/repositories/user_repository.dart';

final userRepository = UserRepository();

final pages = {
  'login': LoginPage(userRepository: userRepository),
  'home': HomePage(),
  'splash': SplashPage(),
  '' : LoginPage(userRepository: userRepository),
};

final routes = {
  // When navigating to the "/signup" route, build the SignupPage BLOC.
  '/signup': (context) => SignUpPage(),
  // When navigating to the "/second" route, build the ProfilePage BLOC.
  // '/profile': (context) => ProfilePage(),
  // '/recover-password': (context) => RecoverPasswordPage(),
};
*/